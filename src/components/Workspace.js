import React, { PropTypes, Component } from 'react'

const styles = {
  float: 'left',
  height: '100%',
  width: '500px'
}

export default class Workspace extends Component {
  render() {
    return (
      <div style={ styles } />
    )
  }
}
